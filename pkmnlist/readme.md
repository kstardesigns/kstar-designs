# Run project locally
1. In pkmnlist folder, run `npm install`. 
2. Run `npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch` if making Tailwind changes. (Ignore caniuse-lite warning.)
3. Right click index.html → Open with Live Server (or just open in Chrome if Live Server is acting up)

# Latest to live site
1. Just push to github.